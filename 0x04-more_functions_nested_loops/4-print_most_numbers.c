#include "main.h"

/**
 * print_must_numbers - print 0 to 9
 *
 * exept 2 and 4
 */

void print_most_number(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		if (nu)
		{
			_putchar(num + '0');
		}
	}
	_putchar('\n');
}
